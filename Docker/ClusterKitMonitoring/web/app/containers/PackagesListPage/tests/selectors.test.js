import {
  selectPackagesListPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectPackagesListPage();

describe('selectPackagesListPage', () => {

});
