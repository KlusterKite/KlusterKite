/*
import expect from 'expect';
import actorsTreePageReducer from '../reducer';
import { fromJS } from 'immutable';

describe('actorsTreePageReducer', () => {
  it('returns the initial state', () => {
    expect(actorsTreePageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
*/
